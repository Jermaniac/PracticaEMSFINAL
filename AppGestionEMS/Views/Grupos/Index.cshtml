@model IEnumerable<AppGestionEMS.Models.Grupos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mañanatarde)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mañanatarde)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GrupoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GrupoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GrupoId })
        </td>
    </tr>
}

</table>
